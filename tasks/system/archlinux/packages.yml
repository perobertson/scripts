---
  - name: Archlinux - Install packages
    pacman:
      name: "{{ item }}"
      state: present
    loop:
      - bash
      - bash-completion
      - code
      - curl
      - docker
      - exa
      - fd
      - flameshot
      - flatpak
      - gcc
      - gdb
      - gzip
      - jq
      - keybase
      - keybase-gui
      - make
      - nodejs
      - npm
      - shellcheck
      - tk # windowing toolkit needed for gitk
      - tree
      - wget
      - xz
      - zsh
